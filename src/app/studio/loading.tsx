import { AppRouteLoading } from "@/components/app/AppRouteLoading";

export default function StudioLoading() {
  return (
    <AppRouteLoading
      eyebrow="Studio"
      title="Keeping your workspace warm"
    />
  );
}
